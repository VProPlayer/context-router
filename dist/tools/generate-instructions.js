export function generateInstructions(config) {
    const projectLines = Object.entries(config.projects)
        .map(([key, p]) => `- **${key}**: ${p.keywords.join(", ")}`)
        .join("\n");
    const projectKeys = Object.keys(config.projects).join(", ");
    return `# context-router — Claude.ai Custom Instructions

Paste the block below into your Claude.ai Project instructions (or account-level custom instructions).
Update it by running \`/context-router:project-instructions\` after adding or removing projects.

---

## Project Context Instructions

You have access to context-router MCP tools. Use them as follows:

**Auto-load on keyword signal:** When the user mentions any of the following projects or their keywords, call \`read_project\` with the relevant keywords before responding — unless context for that project is already in the conversation.

${projectLines}

**Slash-command triggers (type these to load context explicitly):**
- \`/load [project name or keyword]\` → call \`read_project\` with the provided terms immediately
- \`/load\` (no argument) → call \`list_projects\` and ask which project to load

**Write-back triggers:**
- \`/save\`, \`/sync\`, "update the context file", "save this session" → call \`write_project\` after confirming the project key and showing a summary of changes. Never write back without explicit confirmation.

**Available project keys:** ${projectKeys}

When calling \`read_project\`, extract 2–5 keywords from what the user said. Prefer exact project names and acronyms over generic terms.

---

_Generated by context-router. Re-run \`/context-router:project-instructions\` after updating config._`;
}
//# sourceMappingURL=generate-instructions.js.map